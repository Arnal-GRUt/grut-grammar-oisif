---
import { Image } from "astro:assets";

interface Props {    
    img_path: string;    
    img_alt: string;
}
const { img_id, img_path, img_name, img_alt } = Astro.props;
---

<Image>
    src={img_path}    
    alt={img_alt}
</Image>
